{
  "name": "ref-ai-agent-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polls agent.tasks and runs Cursor SDK locally against the app repo",
  "scripts": {
    "start": "node src/worker.js",
    "once": "node src/worker.js --once"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.0",
    "dotenv": "^16.4.7",
    "pg": "^8.14.1"
  }
}
